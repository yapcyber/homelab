# Wanderer — copier vers .env et remplacer les valeurs.
MEILI_MASTER_KEY=CHANGE_ME              # openssl rand -base64 32
POCKETBASE_ENCRYPTION_KEY=CHANGE_ME     # openssl rand -hex 16 (32 caractères exactement)

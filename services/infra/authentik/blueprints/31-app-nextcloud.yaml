# Nextcloud — provider OIDC + application + restriction au groupe "family".
version: 1
metadata:
  name: app-nextcloud-oidc
entries:
  - id: provider-nextcloud
    model: authentik_providers_oauth2.oauth2provider
    identifiers:
      name: nextcloud-proxy
    attrs:
      name: nextcloud-proxy
      client_type: confidential
      client_id: T2d92MaPvVUysa96kYqnkASZe1LK8SAgEtGhcEG0
      client_secret: !Env NEXTCLOUD_OIDC_CLIENT_SECRET
      sub_mode: hashed_user_id
      issuer_mode: per_provider
      include_claims_in_id_token: true
      authorization_flow: !Find [authentik_flows.flow, [slug, default-provider-authorization-implicit-consent]]
      invalidation_flow: !Find [authentik_flows.flow, [slug, default-provider-invalidation-flow]]
      signing_key: !Find [authentik_crypto.certificatekeypair, [name, "authentik Self-signed Certificate"]]
      property_mappings:
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, openid]]
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, email]]
        - !Find [authentik_providers_oauth2.scopemapping, [scope_name, profile]]
      redirect_uris:
        - url: https://cloud.yapserver.fr/apps/user_oidc/code
          matching_mode: strict
        - url: https://cloud.yapserver.fr/index.php/apps/user_oidc/code
          matching_mode: strict
  - id: app-nextcloud
    model: authentik_core.application
    identifiers:
      slug: nextcloud
    attrs:
      name: nextcloud
      slug: nextcloud
      provider: !KeyOf provider-nextcloud
      policy_engine_mode: any
  - model: authentik_policies.policybinding
    identifiers:
      target: !KeyOf app-nextcloud
      group: !Find [authentik_core.group, [name, family]]
    attrs:
      order: 1
      enabled: true
